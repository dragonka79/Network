1. Verify inventory: 
    ansible-inventory --list
    ansible-inventory -i hosts.yml --list

2. Ping the hosts in 'nxos' group:
    ansible nxos -m ping -i hosts.yml

3. Run a baseline.yml playbook:
    ansible-playbook -i ./lab/hosts.yml playbooks/baseline.yml





